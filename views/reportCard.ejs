<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Term Progress Report - <%= student.name %></title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        padding: 15px;
        min-height: 100vh;
      }

      .report-container {
        max-width: 900px;
        margin: 0 auto;
        background: white;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
        border-radius: 8px;
        overflow: hidden;
      }

      .header {
        background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
        color: white;
        padding: 10px 15px;
        text-align: center;
      }

      .school-name {
        font-size: 20px;
        font-weight: bold;
        margin-bottom: 2px;
        text-transform: uppercase;
        letter-spacing: 1px;
      }

      .school-address {
        font-size: 10px;
        margin-bottom: 1px;
        opacity: 0.9;
      }

      .report-title {
        font-size: 15px;
        margin-top: 5px;
        padding-top: 5px;
        border-top: 1px solid rgba(255, 255, 255, 0.3);
        font-weight: 600;
      }

      .session-info {
        font-size: 11px;
        margin-top: 2px;
      }

      .student-info {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 6px;
        padding: 8px 15px;
        background: #f8f9fa;
        border-bottom: 2px solid #2a5298;
        font-size: 10px;
      }

      .info-item {
        display: flex;
        align-items: center;
      }

      .info-label {
        font-weight: 600;
        color: #2a5298;
        margin-right: 5px;
        min-width: 80px;
      }

      .info-value {
        color: #333;
        font-weight: 500;
      }

      .content {
        padding: 10px 15px;
      }

      .section-title {
        font-size: 13px;
        font-weight: bold;
        color: #2a5298;
        margin-bottom: 6px;
        padding-bottom: 3px;
        border-bottom: 2px solid #e0e0e0;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 8px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      }

      thead {
        background: linear-gradient(135deg, #2a5298 0%, #1e3c72 100%);
        color: white;
      }

      th {
        padding: 5px 4px;
        text-align: center;
        font-size: 9px;
        font-weight: 600;
        border-right: 1px solid rgba(255, 255, 255, 0.2);
      }

      td {
        padding: 4px;
        text-align: center;
        border-bottom: 1px solid #e0e0e0;
        font-size: 9px;
      }

      td:first-child {
        text-align: left;
        font-weight: 500;
        color: #333;
      }

      .grade-a { color: #28a745; font-weight: bold; }
      .grade-b { color: #007bff; font-weight: bold; }
      .grade-c { color: #ffc107; font-weight: bold; }
      .grade-d { color: #fd7e14; font-weight: bold; }
      .grade-f { color: #dc3545; font-weight: bold; }

      .summary-section {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 8px;
      }

      .summary-card {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        padding: 8px;
        border-radius: 5px;
        text-align: center;
      }

      .summary-value {
        font-size: 16px;
        font-weight: bold;
        margin-top: 2px;
      }

      .result-box {
        display: grid;
        grid-template-columns: 2fr 1fr;
        padding: 6px 10px;
        background: #f8f9fa;
        border-radius: 5px;
        margin: 10px 0;
        font-size: 10px;
      }

      .remarks-section {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 8px;
      }

      .remark-box {
        border: 1px solid #e0e0e0;
        border-radius: 5px;
        padding: 6px;
      }

      .footer {
        background: #2a5298;
        color: white;
        text-align: center;
        padding: 6px;
        font-size: 8px;
      }

      .print-btn {
        position: fixed;
        top: 15px;
        right: 15px;
        background: #28a745;
        color: white;
        border: none;
        padding: 10px 20px;
        border-radius: 5px;
        font-size: 13px;
        cursor: pointer;
        z-index: 1000;
      }

      @media print {
        .print-btn {
          display: none;
        }
      }
    </style>
  </head>

  <body>
    <button class="print-btn" onclick="window.print()">🖨️ Print</button>

    <div class="report-container">
      <div class="header">
        <div class="school-name">Alcimfa International Schools</div>
        <div class="school-address">Behind Int'l Livestock Market, Jibi Dei-Dei, Abuja | info@alcimfa.edu</div>
        <div class="report-title">TERM PROGRESS REPORT</div>
        <div class="session-info"><%= session %> Academic Session</div>
      </div>

      <div class="student-info">
        <div class="info-item"><span class="info-label">Name:</span> <span class="info-value"><%= student.name %></span></div>
        <div class="info-item"><span class="info-label">Adm. No:</span> <span class="info-value"><%= student.regNumber %></span></div>
        <div class="info-item"><span class="info-label">Class:</span> <span class="info-value"><%= student.classLevel %></span></div>
        <div class="info-item"><span class="info-label">Section:</span> <span class="info-value"><%= student.section %></span></div>
        <div class="info-item"><span class="info-label">Exam:</span> <span class="info-value"><%= student.examName %></span></div>
        <div class="info-item"><span class="info-label">Gender:</span> <span class="info-value"><%= student.gender %></span></div>
      </div>

      <div class="content">
        <div class="section-title">Academic Performance</div>

        <table>
          <thead>
            <tr>
              <th>Subject</th>
              <th>CA1</th>
              <th>CA2</th>
              <th>Exam</th>
              <th>Total</th>
              <th>Grade</th>
              <th>Remark</th>
            </tr>
          </thead>
          <tbody>
            <% subjects.forEach(s => { %>
            <tr>
              <td><%= s.name %></td>
              <td><%= s.ca1 %></td>
              <td><%= s.ca2 %></td>
              <td><%= s.exam %></td>
              <td><%= s.total %></td>
              <td class="grade-<%= s.grade.toLowerCase() %>"><%= s.grade %></td>
              <td><%= s.remark %></td>
            </tr>
            <% }) %>
          </tbody>
        </table>

        <div class="summary-section">
          <div class="summary-card">
            <div class="summary-label">Grand Total</div>
            <div class="summary-value"><%= summary.grandTotal %>/<%= summary.maxMarks %></div>
          </div>
          <div class="summary-card">
            <div class="summary-label">Average</div>
            <div class="summary-value"><%= summary.average %>%</div>
          </div>
          <div class="summary-card">
            <div class="summary-label">GPA</div>
            <div class="summary-value"><%= summary.gpa %></div>
          </div>
        </div>

        <div class="result-box">
          <div><strong>Total In Words:</strong> <%= summary.totalInWords %></div>
          <div style="text-align:right;">
            <strong>Result:</strong>
            <span style="color:<%= summary.resultStatus === 'Pass' ? '#28a745' : '#dc3545' %>"><%= summary.resultStatus %></span>
          </div>
        </div>

        <div style="font-size:10px; margin-bottom:10px;">
          <strong>Grading Scale:</strong>
          <% gradingScale.forEach((g, i) => { %>
            <%= g.grade %> (<%= g.min %>-<%= g.max %>)<%= i < gradingScale.length -1 ? ' | ' : '' %>
          <% }) %>
        </div>

        <div class="section-title">Remarks</div>

        <div class="remarks-section">
          <div class="remark-box">
            <div class="remark-title">Class Teacher:</div>
            <div class="remark-text"><%= remarks.teacher %></div>
          </div>
          <div class="remark-box">
            <div class="remark-title">Head of School:</div>
            <div class="remark-text"><%= remarks.headOfSchool %></div>
          </div>
        </div>
      </div>

      <div class="footer">
        Official Document - Alcimfa International Schools | Generated <%= new Date().toLocaleDateString() %>
      </div>
    </div>
  </body>
</html>
