<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Term Progress Report - <%= student.name %></title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      @page {
        size: A4;
        margin: 10mm;
      }

      body {
        font-family: 'Arial', sans-serif;
        background: #f0f4f8;
        padding: 10px;
      }

      .report-container {
        max-width: 210mm;
        margin: 0 auto;
        background: #e8f5e9;
        border: 2px solid #2e7d32;
        padding: 0;
      }

      /* Compact Header */
      .header {
        text-align: center;
        padding: 8px 15px;
        border-bottom: 3px solid #1565c0;
        background: #e8f5e9;
      }

      .header-top {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 5px;
      }

      .school-logo {
         width: 50px;
        height: 50px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-weight: bold;
        font-size: 20px;
      }

      .school-info {
        flex: 1;
        text-align: center;
      }

      .school-name {
        font-size: 16px;
        font-weight: bold;
        margin-bottom: 2px;
      }

      .school-address {
        font-size: 9px;
        margin-bottom: 3px;
      }

      .report-title {
        background: #1565c0;
        color: white;
        padding: 4px 10px;
        font-size: 11px;
        font-weight: bold;
        display: inline-block;
        letter-spacing: 1px;
      }

      /* Compact Student Info */
      .student-section {
        padding: 10px 15px;
        border-bottom: 2px solid #ccc;
      }

      .student-grid {
        display: grid;
        grid-template-columns: 90px 1fr;
        gap: 12px;
      }

      .student-photo {
        width: 90px;
        height: 110px;
        border: 2px solid #333;
        object-fit: cover;
        background: #f5f5f5;
      }

      .student-photo-placeholder {
        width: 90px;
        height: 110px;
        border: 2px dashed #999;
        display: flex;
        align-items: center;
        justify-content: center;
        background: #fafafa;
        font-size: 10px;
        color: #666;
        text-align: center;
      }

      .student-details {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 5px 15px;
        font-size: 10px;
        align-content: start;
      }

      .detail-row {
        display: flex;
        gap: 5px;
      }

      .detail-label {
        font-weight: bold;
        min-width: 80px;
      }

      .detail-value {
        color: #333;
      }

      /* General Notice */
      .notice-section {
        background: #fff9c4;
        padding: 6px 15px;
        font-size: 8px;
        text-align: center;
        border-bottom: 1px solid #f9a825;
      }

      .notice-title {
        font-weight: bold;
        margin-bottom: 2px;
      }

      /* Compact Table */
      .content {
        padding: 10px 15px;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        font-size: 9px;
        margin-bottom: 8px;
      }

      thead {
        background: #333;
        color: white;
      }

      th {
        padding: 5px 3px;
        text-align: center;
        font-weight: bold;
        border: 1px solid #666;
      }

      th:first-child {
        text-align: left;
      }

      td {
        padding: 4px 3px;
        text-align: center;
        border: 1px solid #ddd;
      }

      td:first-child {
        text-align: left;
        font-weight: 500;
      }

      tbody tr:nth-child(even) {
        background: #f9f9f9;
      }

      /* Grade Colors */
      .grade-a { color: #2e7d32; font-weight: bold; }
      .grade-b { color: #1976d2; font-weight: bold; }
      .grade-c { color: #f57c00; font-weight: bold; }
      .grade-d { color: #d32f2f; font-weight: bold; }
      .grade-f { color: #c62828; font-weight: bold; }

      /* Compact Summary */
      .summary-row {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 8px;
        margin-bottom: 8px;
        font-size: 9px;
      }

      .summary-box {
        border: 1px solid #333;
        padding: 5px;
        text-align: center;
      }

      .summary-label {
        font-weight: bold;
        margin-bottom: 2px;
        font-size: 8px;
      }

      .summary-value {
        font-size: 11px;
        font-weight: bold;
      }

      /* Result Status */
      .result-line {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 6px 10px;
        background: #f5f5f5;
        border: 1px solid #ddd;
        margin-bottom: 6px;
        font-size: 9px;
      }

      .result-status {
        font-weight: bold;
        padding: 3px 12px;
        border-radius: 3px;
      }

      .result-pass {
        background: #c8e6c9;
        color: #2e7d32;
      }

      .result-fail {
        background: #ffcdd2;
        color: #c62828;
      }

      /* Grading Scale */
      .grading-box {
        border: 1px solid #333;
        padding: 6px 10px;
        margin-bottom: 8px;
        font-size: 8px;
      }

      .grading-title {
        font-weight: bold;
        margin-bottom: 3px;
      }

      .grade-table {
        display: grid;
        grid-template-columns: repeat(5, 1fr);
        gap: 5px;
        text-align: center;
      }

      .grade-item {
        border: 1px solid #ddd;
        padding: 3px;
        background: #fafafa;
      }

      /* Compact Remarks */
      .remarks-section {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 10px;
        margin-bottom: 8px;
      }

      .remark-box {
        border: 1px solid #333;
        padding: 6px;
        min-height: 50px;
        font-size: 8px;
      }

      .remark-title {
        font-weight: bold;
        margin-bottom: 3px;
        font-size: 9px;
      }

      .remark-content {
        font-style: italic;
        line-height: 1.3;
      }

      .signature-line {
        margin-top: 20px;
        border-top: 1px solid #333;
        padding-top: 3px;
        font-size: 8px;
        text-align: center;
      }

      /* Footer */
      .footer {
        text-align: center;
        padding: 5px;
        font-size: 7px;
        color: #666;
        font-style: italic;
      }

      /* Print Button */
      .print-btn {
        position: fixed;
        top: 20px;
        right: 20px;
        background: #2e7d32;
        color: white;
        border: none;
        padding: 10px 20px;
        border-radius: 5px;
        font-size: 12px;
        cursor: pointer;
        z-index: 1000;
        box-shadow: 0 4px 8px rgba(0,0,0,0.2);
      }

      @media print {
        body {
          background: white;
          padding: 0;
        }

        .print-btn {
          display: none;
        }

        .report-container {
          border: none;
          max-width: 100%;
          page-break-after: avoid;
        }
      }
    </style>
  </head>

  <body>
    <button class="print-btn" onclick="window.print()">🖨️ Print</button>

    <div class="report-container">
      <!-- Header -->
      <div class="header">
        <div class="header-top">
          <div class="school-logo">
        <img src="\img\al-cimfa logo.svg?height=80&width=80" alt="Excellence Academy Logo" class="logo-img" /></div>
          <div class="school-info">
             <div class="school-name">Alcimfa International Schools</div>
        <div class="school-address">Plot 123 Huse, Abuja FCT</div>
        <div class="school-address">Alcimfac@gmail.com</div>
          </div>
          <div style="width: 50px;"></div>
        </div>
        <div class="report-title">TERM PROGRESS REPORT (<%= session %> ACADEMIC SESSION)</div>
      </div>

      <!-- Student Info -->
      <div class="student-section">
        <div class="student-grid">
          <div>
            <% if (student.photo) { %>
              <img src="<%= student.photo %>" alt="Photo" class="student-photo" />
            <% } else { %>
              <div class="student-photo-placeholder">Photo</div>
            <% } %>
          </div>
          
          <div class="student-details">
            <div class="detail-row">
              <span class="detail-label">Student Name:</span>
              <span class="detail-value"><%= student.name %></span>
            </div>
            <div class="detail-row">
              <span class="detail-label">Admission No:</span>
              <span class="detail-value"><%= student.admissionNo %></span>
            </div>
            <div class="detail-row">
              <span class="detail-label">Class:</span>
              <span class="detail-value"><%= student.class %></span>
            </div>
            <div class="detail-row">
              <span class="detail-label">Section:</span>
              <span class="detail-value"><%= student.term %></span>
            </div>
            <div class="detail-row">
              <span class="detail-label">Gender:</span>
              <span class="detail-value"><%= student.gender %></span>
            </div>
          </div>
        </div>
      </div>

      <!-- General Notice -->
      <div class="notice-section">
        <div class="notice-title">GENERAL NOTE!</div>
        <div>The 2nd Term (2025/2026 Academic Session) begins in 2026. School fees payment is due within 2 weeks of resumption.</div>
      </div>

      <!-- Content -->
      <div class="content">
        <!-- Subjects Table -->
        <table>
          <thead>
            <tr>
              <th>Subjects</th>
              <th>1st C.A</th>
              <th>2nd C.A</th>
              <th>Exam</th>
              <th>Total</th>
              <th>Grade</th>
              <th>Point</th>
              <th>Remark</th>
            </tr>
          </thead>
          <tbody>
            <% subjects.forEach(s => { %>
            <tr>
              <td><%= s.name %></td>
              <td><%= s.ca1 %>/15</td>
              <td><%= s.ca2 %>/15</td>
              <td><%= s.exam %>/70</td>
              <td><%= s.total %>/100</td>
              <td class="grade-<%= s.grade.toLowerCase() %>"><%= s.grade %></td>
              <td>100.00</td>
              <td><%= s.remark %></td>
            </tr>
            <% }) %>
          </tbody>
        </table>

        <!-- Summary -->
        <div class="summary-row">
          <div class="summary-box">
            <div class="summary-label">GRAND TOTAL</div>
            <div class="summary-value"><%= summary.grandTotal %>/1000</div>
          </div>
          <div class="summary-box">
            <div class="summary-label">GRAND TOTAL IN WORDS</div>
            <div class="summary-value" style="font-size: 8px;"><%= summary.totalInWords %></div>
          </div>
          <div class="summary-box">
            <div class="summary-label">Average : <%= summary.average %>%</div>
            <div class="summary-value">GPA : <%= summary.gpa %></div>
          </div>
          <div class="summary-box">
            <div class="summary-label">No. Attendance</div>
            <div class="summary-value">0.00%</div>
          </div>
        </div>

        <!-- Result Status -->
        <div class="result-line">
          <span><strong>RESULT:</strong></span>
          <span class="result-status <%= summary.resultStatus === 'Pass' ? 'result-pass' : 'result-fail' %>">
            <%= summary.resultStatus %>
          </span>
        </div>

        <!-- Grading Scale -->
        <div class="grading-box">
          <div class="grading-title">Grading Scale</div>
          <div class="grade-table">
            <% gradingScale.forEach(g => { %>
            <div class="grade-item">
              <strong><%= g.grade %></strong><br>
              <%= g.min %>% - <%= g.max %>%
            </div>
            <% }) %>
          </div>
        </div>

        <!-- Remarks -->
        <div class="remarks-section">
          <div class="remark-box">
            <div class="remark-title">Head of School's Remarks:</div>
            <div class="remark-content"><%= remarks.headOfSchool %></div>
            <div class="signature-line">Head of School's Signature: [_____________]</div>
          </div>
          <div class="remark-box">
            <div class="remark-title">Class Teacher's Remarks:</div>
            <div class="remark-content"><%= remarks.teacher %></div>
            <div class="signature-line">Class Teacher's Signature: [_____________]</div>
          </div>
        </div>

        <!-- Footer Note -->
        <div class="footer">
          (Work hard next time, you can do better.)
        </div>
      </div>
    </div>
  </body>
</html>
